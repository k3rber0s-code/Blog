extends layout

block content
    div.posts
        h1=title
        h2 #!{tag.name}

    div.container
        section.articles
            ul.article-ul
                each post in posts
                    li
                        a(href=("/" + post.url))
                            span= post.title
                            span.article-list__date= post.creationDate.toDateString()